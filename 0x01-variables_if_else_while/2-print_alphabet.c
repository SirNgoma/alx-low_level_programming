#include <stdio.h>

/**
 * main - create alpha
 * Return: lowercase alpha
 */

int main(void)
{
	int alpha;

	for (alpha = 97; alpha <= 122; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
