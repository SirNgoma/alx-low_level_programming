#include <stdio.h>

/**
 * main - lower and uppercase alphabets
 * Return; alphabets in lower and upper case
 */

int main(void)
{

	int lo;
	int up;

	for (lo = ; lo <= ; lo++)
	{
		putchar(lo);
	}

	for (up = ; up <= ; up++)
	{
		putchar(up);

	}

	return(0);
}
