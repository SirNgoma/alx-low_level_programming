#include "main.h"

/**
 * print_alphabet - prints alphanets
 * Return: rrtruns letter
 */

void print_alphabet(void)
{

	int ch;

	for (ch = ; ch <=  ; ch++)
	{
		_putchar('%d',ch);
	}

	return (0);
}
